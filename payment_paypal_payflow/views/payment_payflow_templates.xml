<?xml version="1.0" encoding="utf-8"?>
<!--
    @author Chintan Ambaliya <chintan.ambaliya@bistasolutions.com>
-->
<odoo>
    <data>
        <template id="sale_order_portal_template" name="Sales Order Portal Template" inherit_id="sale.sale_order_portal_template">
            <xpath expr="//t[@t-call='payment.payment_tokens_list']" position="inside">
                <t t-set="order_id" t-value="sale_order.id"/>
            </xpath>
        </template>

        <template id="payflow_form"/>

        <template id="payflow_s2s_form">
            <input type="hidden" name="data_set" data-create-route="/payment/paypal_payflow/s2s/create_json_3ds"/>
            <input type="hidden" name="acquirer_id" t-att-value="id"/>
            <input type="hidden" name="order_id" t-att-value="order_id"/>
            <input t-if="return_url" type="hidden" name="return_url" t-att-value="return_url"/>
            <input t-if="partner_id" type="hidden" name="partner_id" t-att-value="partner_id"/>
            <div t-attf-class="row mt8 #{'' if bootstrap_formatting else 'o_card_brand_detail'}">
                <div t-att-class="'form-group col-lg-12' if bootstrap_formatting else 'form-group'">
                    <input type="tel" name="cc_number" id="cc_number" class="form-control"
                           placeholder="Card number" data-is-required="true"/>
                    <div class="card_placeholder"/>
                    <div class="visa"/>
                    <input type="hidden" name="cc_brand" value=""/>
                </div>
                <div t-att-class="'form-group col-lg-5' if bootstrap_formatting else 'form-group'">
                    <input type="text" name="cc_holder_name" id="cc_holder_name" class="form-control"
                           placeholder="Cardholder name" data-is-required="true"/>
                </div>
                <div t-att-class="'form-group col-lg-3' if bootstrap_formatting else 'form-group'">
                    <input type="text" name="cc_expiry" id="cc_expiry" class="form-control" maxlength="7"
                           placeholder="Expires (MM / YY)" data-is-required="true"/>
                </div>
                <div t-att-class="'form-group col-lg-4' if bootstrap_formatting else 'form-group'">
                    <input type="password" name="cc_cvc" id="cc_cvc" class="form-control" maxlength="4"
                           placeholder="CVC" data-is-required="true"/>
                </div>
            </div>
        </template>
    </data>
</odoo>
